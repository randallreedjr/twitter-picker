<h3>Tweets per campaign</h3>
<%= column_chart Tweet.joins(:campaign).where(:campaigns => {:user_id => current_user.id }).group(:hashtag).count, colors: ["#139AE9"] %>

<h3>Tweets over time</h3>
<%= line_chart Tweet.joins(:campaign).where(:campaigns => {:user_id => current_user.id }).group_by_day(:entry_time).count, colors: ["#139AE9"] %>

<h3>Total Reach per Campaign</h3>
<%= column_chart Tweeter.joins(:campaign).where(:campaigns => {:user_id => current_user.id}).group(:hashtag).sum(:followers_count), colors: ["#139AE9"] %>
<%= pie_chart Tweeter.joins(:campaign).where(:campaigns => {:user_id => current_user.id}).group(:hashtag).sum(:followers_count), colors: ["#139AE9", '#FAD000', '#FF1493']  %>

